# Spec Format Standard

> **TODO** — placeholder per Epic 2 master plan §Out of scope.
>
> Substantive authoring deferred to post-cleanup follow-up. This file
> exists to reserve the canonical location for the spec-format
> standard under `standards/spec/`; the content (canonical frontmatter
> shape, kind grammar reference, V/W code conventions, etc.) is
> authored in a separate spec after Epic 2 lands.
>
> See `standards/spec/constitution.md` for the constitutional baseline
> and `standards/spec/contract.md` for the precedence rules that
> govern the eventual content of this file.
