# Implementation Plan: allow-invalid contract

**Spec**: [spec.md](./spec.md)

## Summary

Add allow-invalid policy fixtures and integration tests that lock behavior with and without `--allow-invalid` for list/show/status-report, including malformed-shape boundaries that must still fail.
