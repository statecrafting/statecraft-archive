# Implementation Plan: pointer

The implementation plan for the `oap-bootstrap` CLI (milestones M1+, stack, test
strategy, risks) is authored in the **oap-bootstrap repository** alongside its
authoritative spec `001-oap-instance-bootstrap-cli`. See that repo.

The only OAP-side work is FR-040 (fork-parameterization of the Hetzner
`setup.sh` flux-bootstrap seam), specified in this directory's `spec.md` and
landing in the OAP tree directly. It is the oap-bootstrap design's `M0`
milestone.
