# Tasks: Schema-Parity Walker — Rebuild for Hand-Rolled Validators

**Input**: `/specs/125-schema-parity-walker-rebuild/`
**Prerequisites**: spec.md, plan.md
**Phases**: 0 (Foundations) → 1 (Descriptor) → 2 (Consistency test) → 3 (Walker) → 4 (CI integration) → 5 (Closure)

Tasks are grouped by phase per `plan.md`. `[P]` = can run in parallel with other `[P]` tasks in the same phase. Each phase ends with a checkpoint that gates the commit.

---

## Phase 0 — Foundations

Define the shared `SchemaNode` type used by both the descriptor (in `extractionOutput.ts`) and the walker (in the parity tool).

- [ ] **T001** Decide the home of `SchemaNode`. Two options: (a) declare in `extractionOutput.ts` and re-export, (b) declare in a small new file `tools/oap/schema-parity-check/schema-node.ts` and import into `extractionOutput.ts`. Choose (a) for v1: keeps the parity tool dependency-free and the descriptor next to the validator. Document the choice in a one-line file header.
- [ ] **T002** [P] In `extractionOutput.ts`, add the `SchemaNode` discriminated union:
  ```ts
  export type SchemaNode =
    | { kind: "string" }
    | { kind: "int" }
    | { kind: "number" }
    | { kind: "boolean" }
    | { kind: "unknown" }
    | { kind: "enum"; values: string[] }
    | { kind: "array"; element: SchemaNode }
    | { kind: "tuple"; items: SchemaNode[] }
    | { kind: "object"; fields: Array<{ name: string; required: boolean; type: SchemaNode }> }
    | { kind: "discriminatedUnion"; discriminator: string; variants: Array<{ tag: string; fields: Array<{ name: string; required: boolean; type: SchemaNode }> }> };
  ```
  Field shape matches what the existing zod walker emits for parity-tool fingerprint compatibility.
- [ ] **T003** [P] Audit the current Rust fingerprint shape at `.derived/schema-parity/rust-knowledge-schema.json` (regenerated by `cargo test --manifest-path crates/factory-contracts/Cargo.toml`). Write down a mental map between the JSON's field names and the `SchemaNode` variants. The descriptor (Phase 1) MUST produce a walk result identical to this JSON for `make ci-schema-parity` to go green.

**Checkpoint:** `npx tsc --noEmit` in statecraft passes with the new type defined. No behaviour change yet. Commit: `chore(statecraft, spec-125): SchemaNode discriminated union`.

---

## Phase 1 — Descriptor

Author the plain-data descriptor that mirrors what `Validator` checks.

- [ ] **T010** In `extractionOutput.ts`, add `extractionOutputDescriptor: SchemaNode`. Walk through `validateExtractionOutput` and the helpers (`validateTokenSpend`, `validateAgentRun`, `validatePage`, `validateOutlineEntry`, `validateExtractor`) and translate each `requireString`/`requireInt`/`requireNumber`/`requireObject`/enum check into the equivalent `SchemaNode` shape. Required vs. optional must match exactly: a field that the validator allows to be `undefined` is `required: false`.
- [ ] **T011** Cross-check against `crates/factory-contracts/src/knowledge.rs`. Rebuild the Rust fingerprint (`cargo test --manifest-path crates/factory-contracts/Cargo.toml`) and pretty-print `.derived/schema-parity/rust-knowledge-schema.json`. The descriptor's eventual fingerprint should structurally match — same field names, same types, same enum values, same required/optional flags. If the validator and the Rust mirror disagree, fix the validator first; this spec is not a vehicle for changing the schema.
- [ ] **T012** [P] Make `KNOWLEDGE_SCHEMA_VERSION` continue to be exported (already is). The parity tool reads it; no value change in this PR.
- [ ] **T013** [P] If any helper validator function uses a non-trivial check (e.g. `HEX_64.test`, `Number.isInteger`), capture it as a comment on the descriptor field — the descriptor itself only carries `kind: "string" | "int" | ...`, so the value-shape check is a known gap. Document it explicitly with a `// note: validator additionally enforces …` comment so a future reader knows the descriptor is structural-only.

**Checkpoint:** `npx tsc --noEmit` passes. The descriptor exists and exports cleanly. Commit: `feat(statecraft, spec-125): extractionOutputDescriptor`.

---

## Phase 2 — Consistency test

A vitest case that the descriptor matches what `Validator` actually checks.

- [ ] **T020** Create or extend `platform/services/statecraft/api/knowledge/extractionOutput.test.ts`. Add a `describe("extractionOutputDescriptor consistency")` block.
- [ ] **T021** Build a "minimal valid payload" fixture that the validator accepts. Use it as the baseline.
- [ ] **T022** For every `required: true` field in the descriptor, generate a payload variant with that field deleted and assert `validateExtractionOutput(variant)` throws `ExtractorReturnedMalformedOutputError`. Iterate the descriptor recursively (nested objects, array elements, tuple positions).
- [ ] **T023** For every `required: false` field, generate a payload variant with that field deleted and assert `validateExtractionOutput(variant)` does NOT throw.
- [ ] **T024** [P] For every `kind: "enum"` field, generate a payload variant with the field set to a value NOT in `values` and assert the validator rejects it.
- [ ] **T025** [P] Add a `node:test` fallback or guard if `vitest` is not configured to run the file. Currently statecraft uses vitest; confirm the existing `extractionOutput.test.ts` location runs by default under `npm test`.

**Checkpoint:** `npm test -- extractionOutput.test.ts` passes locally. The added consistency block has at least three test cases (required-missing, optional-missing, enum-out-of-set). Commit: `test(statecraft, spec-125): in-file descriptor↔validator consistency`.

---

## Phase 3 — Walker rewrite

`tools/oap/schema-parity-check/index.mjs` learns to walk descriptors as well as zod.

- [ ] **T030** In `tools/oap/schema-parity-check/index.mjs`, add `function walkDescriptor(node)` that maps `SchemaNode` → the same fingerprint shape the existing `walkType(zod)` produces. Cases must mirror Phase 0's union:
  - `string|int|number|boolean|unknown` → `{ kind }`
  - `enum` → `{ kind: "enum", values: [...sorted] }` (sort to keep order-independent)
  - `array` → `{ kind: "array", element: walk(child) }`
  - `tuple` → `{ kind: "tuple", items: [...walk(each)] }`
  - `object` → `{ kind: "object", fields: [...sorted by name] }`
  - `discriminatedUnion` → `{ kind: "discriminatedUnion", discriminator, variants: [...sorted by tag] }`
- [ ] **T031** Refactor the entry point to dispatch:
  ```js
  const knowledgeNode = mod.extractionOutputDescriptor;
  if (!knowledgeNode || !knowledgeNode.kind) {
    fail(2, "schema-parity-check: extractionOutputDescriptor missing or malformed");
  }
  const tsFingerprint = { version: tsSchemaVersion, root: walkDescriptor(knowledgeNode) };
  ```
  Replace the existing `walkType(extractionOutputSchema)` invocation with the descriptor walk. The `extractionOutputSchema` import + check is removed.
- [ ] **T032** [P] Provenance + stakeholder-doc paths (specs 121 / 122) currently exist only Rust-side per their §8 reserved mode. Keep the existing "Rust-side fingerprint recorded for later" path unchanged — it does not depend on zod and is unaffected by this refactor. Verify by running `bun run tools/oap/schema-parity-check/index.mjs` against a tree where neither TS mirror exists; expect informational lines, not failures.
- [ ] **T033** [P] Delete `function walkType(zod)` and the `unwrap`/`isOptional` zod-tree helpers if no remaining caller exists. If specs 121 / 122 still need them in the same tool, leave them in place behind a comment noting their planned removal. Search inside `index.mjs` for `_def\\.` usages; if zero, the zod walker is dead and goes; if non-zero, it stays.
- [ ] **T034** [P] Update the comment block at the top of `index.mjs` to describe the dispatcher behaviour. The existing comment is correct in spirit but mentions zod walking; update to mention descriptors as the canonical path going forward.
- [ ] **T035** [P] Test: a tiny mock descriptor file fed through `walkDescriptor` produces a fingerprint string-equal to the equivalent zod-walked fingerprint for the same shape. Run as a one-off `node -e ...` script committed under `tools/oap/schema-parity-check/walk_descriptor.test.mjs` (or add to the existing test harness if one exists).

**Checkpoint:** `bun run tools/oap/schema-parity-check/index.mjs` exits 0 on a clean tree. `make ci-schema-parity` exits 0. Drift is detected (T080 in Phase 5 verifies). Commit: `feat(tools, spec-125): schema-parity walker dispatches on descriptor`.

---

## Phase 4 — CI integration

Verify the parity gate goes green; verify it actually catches drift.

- [ ] **T040** Run `make ci-schema-parity` from a clean tree. Expect exit 0 and no diagnostic lines beyond the informational provenance / stakeholder-doc messages.
- [ ] **T041** Drift smoke test: temporarily edit `extractionOutputDescriptor` to rename one field; run `make ci-schema-parity`; expect non-zero exit with a clear `<root>.<field>` diff line. Revert.
- [ ] **T042** [P] In-file consistency drift smoke test: temporarily edit the descriptor to mark a currently-required field as `required: false`; run `npm test -- extractionOutput.test.ts`; expect the consistency block to fail because the validator still throws on missing-required. Revert.
- [ ] **T043** [P] Encore parser regression smoke test: run `cd platform/services/statecraft && npm run gen` (which exercises the Encore TS parser walking the API tree). Expect no parser crashes. The descriptor file imports zero zod symbols — confirm by `! rg "from \"zod" platform/services/statecraft/api/knowledge/extractionOutput.ts`.
- [ ] **T044** [P] `make ci` end-to-end. Now that schema-parity is fixed, the previously-failing gate goes green; assuming all other gates were green pre-spec-125, `make ci` exits 0. If any other gate is red (unrelated regression), surface separately — do NOT bundle the fix into spec 125.

**Checkpoint:** `make ci-schema-parity` green; drift smoke tests pass; `make ci` green (or, if red on an unrelated gate, that gate is separately tracked). Commit: `chore(spec-125): verify ci-schema-parity restored`.

---

## Phase 5 — Closure

Acceptance gates A-1..A-5; spec lifecycle flip.

- [ ] **T050** Verify A-1: `make ci-schema-parity` exits 0 on a clean tree.
- [ ] **T051** Verify A-2: `make ci` exits 0 on a clean tree.
- [ ] **T052** Verify A-3: rename a field in `extractionOutputDescriptor` without touching `crates/factory-contracts/src/knowledge.rs`; `make ci-schema-parity` fails with a clear `<root>.<field>` diff message. Revert.
- [ ] **T053** Verify A-4: rename a field in `extractionOutputDescriptor` without touching `Validator`; `npm test -- extractionOutput.test.ts` fails on the consistency case. Revert.
- [ ] **T054** Verify A-5: `! rg "from \"zod" platform/services/statecraft/api/knowledge/extractionOutput.ts` exits true (no zod import in the file). The Encore parser invariant holds.
- [ ] **T055** Spec frontmatter flip: `status: draft → approved`, `implementation: pending → complete`, add `approved: <today>`. Append a brief Implementation Notes section pointing at the descriptor + dispatcher commits.
- [ ] **T056** `make registry` — recompile spec registry + codebase index. Must be clean.
- [ ] **T057** Commit: `feat(specs): mark spec 125 approved + complete; refresh registry`.

**Final checkpoint:** spec §5 acceptance is fully green; `git log --oneline` shows the per-phase commit trail (one commit per Phase 0..4 + the lifecycle flip = six commits).

---

## Halt conditions

Stop and report up — do NOT continue past these without surfacing:

- The Rust-side fingerprint at `.derived/schema-parity/rust-knowledge-schema.json` does not match the descriptor even after Phase 1's careful translation. This indicates a real schema drift between the validator and the Rust mirror that predates this spec; fixing it is in scope ONLY if the fix is mechanical (rename one field, etc.). Otherwise halt.
- The Encore TS parser crashes after the change. The descriptor MUST NOT trigger the parser bug; if it does, the descriptor file structure needs reshaping (e.g. move `SchemaNode` out into `tools/oap/schema-parity-check/schema-node.ts` per Phase 0 option (b)).
- The dispatcher cannot be made to coexist with the zod walker. If the `walkType` zod paths in the parity tool are too entangled with the entry point to refactor cleanly, halt and surface — a partial dispatcher is worse than the current single-walker code.
- A field in `extractionOutput.ts` is parsed by the validator with a check the descriptor's `SchemaNode` cannot represent (e.g. a custom regex). Document the limitation as a `// note:` comment per T013, do NOT extend `SchemaNode` to express it. The descriptor is intentionally structural-only; value-shape checks remain in the validator and are tested by the consistency block.
