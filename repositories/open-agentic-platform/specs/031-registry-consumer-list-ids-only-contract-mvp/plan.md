# Implementation Plan: list --ids-only contract

**Spec**: [spec.md](./spec.md)

## Summary

Add `--ids-only` to the `list` command with conflict guards against json/compact, add fixture-backed tests for default and filtered outputs, and verify no existing contract drift.
