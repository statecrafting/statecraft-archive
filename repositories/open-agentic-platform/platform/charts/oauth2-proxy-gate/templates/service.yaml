apiVersion: v1
kind: Service
metadata:
  name: {{ include "oauth2-proxy-gate.fullname" . }}
  labels:
    {{- include "oauth2-proxy-gate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "oauth2-proxy-gate.selectorLabels" . | nindent 4 }}
