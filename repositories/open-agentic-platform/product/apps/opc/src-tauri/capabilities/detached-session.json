{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "detached-session",
  "description": "Minimal capability for dynamically-created detached session windows. Only event emission is permitted — no I/O, shell, or plugin access.",
  "windows": ["detached-*"],
  "permissions": [
    "core:default",
    "core:event:allow-emit-to"
  ]
}
