# Rust build output
/target
/node_modules
# Cargo.lock is committed (spec 010 A-2): the ledger's record hashes depend on
# the float formatter the resolved serde_json links.
# napi build artifacts: the platform .node binary and generated JS loader,
# regenerated by `npm run build`.
*.node
index.js
index.d.ts
*.tgz
# Per-platform publish manifests generated by `napi create-npm-dirs` at
# publish time (publish.yml); never committed.
/npm
