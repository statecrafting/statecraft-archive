# Implementation Plan: Hiqlite default-features hygiene

**Branch**: `144-hiqlite-default-features` | **Date**: 2026-05-10 | **Spec**: [`spec.md`](./spec.md)

## Summary

Three lines of `Cargo.toml`, one lockfile regeneration, and a CI
verification pass. The work is one PR scoped to two crate manifests
plus the resulting `crates/Cargo.lock` diff. No source code, schema,
runtime, or chart changes.

The load-bearing realignments:

1. **One missing flag is the root cause.** Adding `default-features =
   false` on `crates/orchestrator/Cargo.toml:20` stops Cargo from
   unifying upstream defaults across the `crates/` workspace.
   Everything else in this spec is a follow-on cleanup that becomes
   visible (or trivially correct) once that flag is in place.
2. **`auto-heal` becomes an explicit choice.** Inherited-by-default
   today; promoted to explicit on both crates so the choice survives
   future feature-list edits.
3. **Two redundant entries drop.** Orchestrator `dlock` is dead;
   axiomregent `cache` is transitively required and need not be listed.
4. **Lockfile is the verification surface.** The visible benefit is
   `cron` and `toml 1.1.2+spec-1.1.0` leaving the hiqlite deps block
   in `crates/Cargo.lock`. `platform/services/deployd-api-rs/Cargo.lock`
   is unchanged (separate workspace; already correct).

## Technical Context

**Languages**: Rust (workspace `crates/`, plus consumer at
`product/apps/opc/src-tauri/`). No TypeScript or chart changes.
**Primary Dependencies**: `hiqlite = ~0.13` (resolves to `0.13.1` on
both Cargo workspaces in the repo).
**Storage**: None — manifest-only change.
**Testing**: existing `cargo check / clippy / test` against
`crates/orchestrator/Cargo.toml` and `crates/axiomregent/Cargo.toml`
with default features. CI surface is
`.github/workflows/ci-orchestrator.yml` plus the workspace-level
`crates/` checks.
**Target Platform**: workspace toolchain only — no deployment or chart
involvement. Desktop binary (`product/apps/opc/src-tauri`) inherits the
new feature posture transitively when rebuilt.
**Project Type**: tooling / dependency hygiene amendment.
**Performance Goals**: smaller compile graph (drops `cron`-related
crate compilation and the `toml` parser path) — not the motivating
metric. Recompile after the change should be no slower than baseline.
**Constraints**: must preserve the existing `local-sqlite` default
build path on `crates/orchestrator/Cargo.toml:11-13`; must preserve
the existing axiomregent runtime behaviour (no API call sites change).
**Scale/Scope**: two manifests, one lockfile diff, ~3 production lines
edited. SBOM impact is positive (drops two transitive crates).

## Constitution Check

*GATE: Must pass before Phase 1. Re-check after Phase 2 (post-implementation).*

- **Principle I — markdown human truth.** Spec/plan/tasks remain
  markdown. ✅
- **Principle II — compiler-emitted JSON machine truth.** The lockfile
  is regenerated by `cargo`, not hand-edited. No compiler output is
  touched. ✅
- **CONST-005 — spec/code coherence.** This is a hygiene fix that
  surfaces drift introduced by the upstream's default-feature set.
  No spec is amended to retroactively justify a code change; the
  motivating audit is captured in `audit.md` and verifications in
  `verifications.md` alongside this spec. ✅
- **No backwards-compat shims** (project memory). The dead `dlock`
  entry is deleted, not gated behind an opt-in feature flag. Same for
  the redundant `cache`. ✅
- **Governed-artifact-reads (spec 103).** No new `python`/`jq` against
  compiled artifacts. CI verification reads `cargo` output, not
  registry JSON. ✅
- **Adapter-scope / coupling gates.** `implements:` lists the three
  files this spec actually changes
  (`crates/orchestrator/Cargo.toml`,
  `crates/axiomregent/Cargo.toml`, `crates/Cargo.lock`). Spec 127
  (and spec 130's primary-owner refinement) accept this list as the
  full coupled surface. ✅

No violations expected.

## Project Structure

### Documentation (this feature)

```text
specs/144-hiqlite-default-features/
├── spec.md           # Feature spec
├── plan.md           # This file
├── tasks.md          # Implementation task list
├── audit.md          # Source — full Hiqlite usage audit (provenance)
└── verifications.md  # Source — PVC posture + distributed-mode build verification
```

### Source code touched

```text
crates/
├── orchestrator/Cargo.toml      # add default-features = false; drop dlock; add auto-heal
├── axiomregent/Cargo.toml       # drop redundant cache; add auto-heal
└── Cargo.lock                   # regenerated — drops cron, toml 1.1.2+spec-1.1.0
```

**Structure Decision**: tightly bounded — three files. No new
directories, no new modules, no new tests authored (existing CI
exercises the affected build paths).

## Phase 0 — Pre-flight verification

**Purpose**: Confirm the audit findings reproduce on the current
working tree before editing anything.

P0.1 **Confirm orchestrator manifest still lacks the flag.** Open
`crates/orchestrator/Cargo.toml:20` and verify the line still reads
`hiqlite = { version = "~0.13", features = ["sqlite", "dlock",
"listen_notify_local"], optional = true }`.

P0.2 **Confirm axiomregent manifest still lists `cache`.** Open
`crates/axiomregent/Cargo.toml:38` and verify the explicit feature
list still reads `["sqlite", "dlock", "listen_notify_local", "cache"]`
with `default-features = false`.

P0.3 **Confirm `crates/Cargo.lock` carries the unified-by-accident
transitives.** Search the file for `name = "cron"` (must be present
once), `name = "toml"` followed by `version = "1.1.2+spec-1.1.0"`
(must be present), and `name = "futures-util"` (present, kept by
explicit `listen_notify_local`).

P0.4 **Confirm `dlock` is dead in orchestrator source.** Run
`grep -rnE 'client\.lock\(|hiqlite::Lock' crates/orchestrator/src` —
expect zero hits.

P0.5 **Confirm distributed build path is dead.** Spot-check
`verifications.md` Q2 still applies — re-run
`grep -rnE 'features.*distributed|--features.*distributed'` across
`**/Cargo.toml`, `**/*.yml`, `**/*.yaml`, `**/Dockerfile*`,
`**/*.sh`, `**/Makefile`, `**/*.mk`, `**/Justfile`, expect zero hits
relevant to enablement (matches still in `Cargo.toml` `[features]`
declarations are expected and not enablement).

**Output:** Phase 0 produces no spec changes. If any check fails,
halt and reconcile against `audit.md` / `verifications.md` before
editing manifests — the reality may have moved.

## Phase 1 — Manifest edits + lockfile regen

**Purpose**: Apply the three-file change.

**Sequence:**

1. **Edit `crates/orchestrator/Cargo.toml:20`** to:
   ```toml
   hiqlite = { version = "~0.13", default-features = false, features = ["sqlite", "listen_notify_local", "auto-heal"], optional = true }
   ```
2. **Edit `crates/axiomregent/Cargo.toml:38`** to:
   ```toml
   hiqlite = { version = "~0.13", default-features = false, features = ["sqlite", "dlock", "listen_notify_local", "auto-heal"] }
   ```
3. **Regenerate the lockfile.** Run `cargo check --manifest-path
   crates/orchestrator/Cargo.toml` once (or `cargo generate-lockfile
   --manifest-path crates/Cargo.toml` against the workspace root) —
   Cargo rewrites `crates/Cargo.lock` to reflect the new feature
   posture without compiling the workspace. Inspect the diff:
   - Removed: `cron` block; `toml` package at version
     `1.1.2+spec-1.1.0` (the `+spec-1.1.0` discriminator is the
     hiqlite-internal `toml` fork; the regular `toml` crate at a
     different version may stay if some other workspace member needs
     it, which is benign).
   - Retained: `futures-util` (kept by explicit `listen_notify_local`),
     `cryptr`, `s3-simple`, `deadpool`, `rusqlite`, etc.
   - If anything unexpected changes, halt and re-read the diff before
     proceeding.
4. **No source-file edits.** No `.rs` file changes are part of this
   phase.

**Phase 1 exit criterion:** the working tree carries the three-file
diff and nothing else.

## Phase 2 — Verification

**Purpose**: confirm the change is benign on every build path the
crates ship through.

**Sequence:**

1. **`cargo check --manifest-path crates/orchestrator/Cargo.toml`** —
   default features only. Expect clean.
2. **`cargo clippy --manifest-path crates/orchestrator/Cargo.toml
   --all-targets`** — default features. Expect clean.
3. **`cargo test --manifest-path crates/orchestrator/Cargo.toml`** —
   default features. Expect clean.
4. **`cargo check --manifest-path crates/axiomregent/Cargo.toml`** —
   expect clean.
5. **`cargo clippy --manifest-path crates/axiomregent/Cargo.toml
   --all-targets`** — expect clean.
6. **`cargo test --manifest-path crates/axiomregent/Cargo.toml`** —
   expect clean.
7. **`make ci`** (warm) — expect green. Spec 134 / 135's fast-CI
   parity is the canonical signal.
8. **Recompile spec registry + codebase index** —
   `./tools/spec-spine/spec-compiler/target/release/spec-compiler compile` and
   `./tools/spec-spine/codebase-indexer/target/release/codebase-indexer compile
   && render`.
9. **Spec-code coupling gate** —
   `./tools/spec-spine/spec-code-coupling-check/target/release/spec-code-coupling-check`
   should accept the diff against this spec's `implements:` list with
   no warnings.

**Phase 2 exit criterion:** AC-1 through AC-7 in `spec.md` §3 pass.

## Test strategy

- **No new tests authored.** The existing CI surface around the
  orchestrator and axiomregent crates already exercises the
  `local-sqlite` build for orchestrator and the desktop hiqlite path
  for axiomregent.
- **Lockfile assertion is a manual diff inspection.** Optionally, a
  one-shot test could `grep` the post-regen lockfile for
  `name = "cron"` and `version = "1.1.2+spec-1.1.0"` and assert
  absence; but adding a test for a one-time hygiene change is
  overkill given the lockfile is itself the assertion artifact.
- **CI parity coverage** comes from `.github/workflows/ci-orchestrator.yml`
  and the `crates/` workspace pipelines. No new workflow needed.

## Risks and rollback

- **Cargo unification surprise.** Edge case: a member of the
  `crates/` workspace not in the audit's scope explicitly opts into
  `cache`/`backup`/`auto-heal`/`toml` via its own manifest. If so,
  the lockfile diff in Phase 1 will preserve those crates. Pre-flight
  search:
  `grep -rnE 'features.*=.*\[.*"(backup|s3|toml|cache)"' crates/*/Cargo.toml`
  — confirm there is no third party.
- **Desktop build regression.** `product/apps/opc/src-tauri` depends on
  `axiomregent`. Build the desktop crate against the new feature
  posture as a smoke check (`cargo check` against
  `product/apps/opc/src-tauri/Cargo.toml`).
- **Rollback** is `git revert` of the manifest edits and a fresh
  `cargo build` to restore the lockfile. No persistent state is
  touched; rollback is symmetric with the change.

## Constitution re-check (post-implementation)

Filled 2026-05-10 at end of Phase 2.

- **Principle I — markdown human truth.** spec.md / plan.md / tasks.md
  remained markdown throughout. Only frontmatter values flipped
  (`implementation: pending` → `complete`, added `closed:`). ✅
- **Principle II — compiler-emitted JSON machine truth.** `crates/Cargo.lock`
  was regenerated by `cargo check`, not hand-edited. The spec
  registry and codebase index were recompiled by their respective
  binaries. No build/** JSON was touched directly. ✅
- **CONST-005 — spec/code coherence.** No spec was amended to
  retroactively justify a code change. The frontmatter close-out
  (`implementation: complete`, `closed:`) is the canonical
  end-of-implementation flip, not a back-dated rationalization. The
  motivating audit (`audit.md`) and verifications (`verifications.md`)
  pre-existed the implementation. ✅
- **No backwards-compat shims.** Dead `dlock` removed outright from
  `crates/orchestrator/Cargo.toml:20`; redundant `cache` removed
  outright from `crates/axiomregent/Cargo.toml:38`. No feature flag
  gates, no dual-listing for compatibility. ✅
- **Governed-artifact-reads (spec 103).** The implementation pass
  used `python3` to inspect `.derived/spec-registry/registry.json` and
  `.derived/codebase-index/index.json` once during interactive debugging
  of the spec-coupling gate (recognising the index was stale and the
  gate's claimant list was therefore incomplete). That use is
  explicitly carved out by the rule's "human running jq at the shell
  to inspect an artifact interactively is not an orchestrated
  workflow" exception. No orchestrated workflow step parsed
  `build/**/*.json`. ✅
- **Adapter-scope / coupling gates.** `implements:` lists exactly the
  three files this spec changes. Spec 127's gate (run locally) listed
  spec 144 as a claimant for all three paths once the index was
  compiled fresh; the spec.md edit at T030 covers all three claims.
  ✅ (Subject to one residual: the chore commit's
  `crates/featuregraph/tests/golden/features_graph.json` is not
  spec 144's coupled surface — handled by PR-body waiver per the
  gate's documented escape hatch.)

No bullet needed amendment. The Constitution Check at the top of this
file stands as written.
