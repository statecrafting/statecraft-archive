name: Rust CI
description: >
  Composite action: cargo check + clippy + test for a single Cargo workspace/crate.
  Caller must run actions/checkout before invoking this action.

inputs:
  manifest-path:
    description: Path to Cargo.toml
    required: true
  workspace:
    description: >
      If 'true', append `--workspace` to cargo invocations so every member of
      the workspace at `manifest-path` is validated in a single pass. Default
      'false' validates only the crate at `manifest-path`. Spec 135 FR-05.
    required: false
    default: 'false'
  clippy-flags:
    description: Extra flags passed to cargo clippy after --
    required: false
    default: '-D warnings'
  cache-shared-key:
    description: shared-key for Swatinem/rust-cache (leave empty to omit)
    required: false
    default: ''
  cache-workspaces:
    description: workspaces for Swatinem/rust-cache (leave empty to omit)
    required: false
    default: ''
  toolchain:
    description: >
      Rust toolchain channel. Default mirrors rust-toolchain.toml at repo root.
      Bump in lockstep with that file — see its header comment for the cross-ref.
    required: false
    default: '1.95.0'

runs:
  using: composite
  steps:
    - name: Rust toolchain
      uses: dtolnay/rust-toolchain@29eef336d9b2848a0b548edc03f92a220660cdb8 # pinned for rust-toolchain.toml lockstep (spec H1)
      with:
        toolchain: ${{ inputs.toolchain }}
        components: clippy, rustfmt

    - name: Cache Rust dependencies (with shared-key and workspaces)
      if: inputs.cache-shared-key != '' && inputs.cache-workspaces != ''
      uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
      with:
        shared-key: ${{ inputs.cache-shared-key }}
        workspaces: ${{ inputs.cache-workspaces }}

    - name: Cache Rust dependencies (with shared-key)
      if: inputs.cache-shared-key != '' && inputs.cache-workspaces == ''
      uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
      with:
        shared-key: ${{ inputs.cache-shared-key }}

    - name: Cache Rust dependencies (with workspaces)
      if: inputs.cache-shared-key == '' && inputs.cache-workspaces != ''
      uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2
      with:
        workspaces: ${{ inputs.cache-workspaces }}

    - name: Cache Rust dependencies
      if: inputs.cache-shared-key == '' && inputs.cache-workspaces == ''
      uses: Swatinem/rust-cache@42dc69e1aa15d09112580998cf2ef0119e2e91ae # v2

    - name: cargo check
      shell: bash
      env:
        CARGO_TERM_COLOR: always
      run: cargo check ${{ inputs.workspace == 'true' && '--workspace' || '' }} --manifest-path ${{ inputs.manifest-path }}

    - name: cargo clippy
      shell: bash
      env:
        CARGO_TERM_COLOR: always
      run: cargo clippy ${{ inputs.workspace == 'true' && '--workspace' || '' }} --manifest-path ${{ inputs.manifest-path }} -- ${{ inputs.clippy-flags }}

    - name: cargo test
      shell: bash
      env:
        CARGO_TERM_COLOR: always
      run: cargo test ${{ inputs.workspace == 'true' && '--workspace' || '' }} --manifest-path ${{ inputs.manifest-path }}
